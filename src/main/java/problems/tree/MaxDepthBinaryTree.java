package problems.tree;

import exceptions.NotImplementedException;
import structures.tree.BinaryTreeNode;

/**
 * Dada la raíz de un árbol binario, devuelve su profundidad máxima.
 * La profundidad máxima es el número de nodos a lo largo del camino más largo
 * desde la raíz hasta el nodo hoja más lejano.
 *
 * <pre>
 * Ejemplo:
 *        3
 *       / \
 *      9  20
 *        /  \
 *       15   7
 *
 * Profundidad máxima: 3
 * </pre>
 */
public class MaxDepthBinaryTree {

    /**
     * Resuelve el problema.
     *
     * @param <T> El tipo de dato almacenado en el árbol.
     * @param root La raíz del árbol binario.
     * @return La profundidad máxima del árbol.
     */
    public <T> int solve(BinaryTreeNode<T> root) {
        throw new NotImplementedException();
    }
}
