package problems.tree;

import exceptions.NotImplementedException;
import structures.tree.BinaryTreeNode;

/**
 * Dada la raíz de un árbol binario, inviértelo (intercambia los hijos izquierdo
 * y derecho de todos los nodos) y devuelve su raíz.
 *
 * <pre>
 * Ejemplo:
 *        4                  4
 *      /   \              /   \
 *     2     7     =>     7     2
 *    / \   / \          / \   / \
 *   1   3 6   9        9   6 3   1
 * </pre>
 */
public class InvertBinaryTree {

    /**
     * Resuelve el problema.
     *
     * @param <T> El tipo de dato almacenado en el árbol.
     * @param root La raíz del árbol binario.
     * @return La raíz del árbol binario invertido.
     */
    public <T> BinaryTreeNode<T> solve(BinaryTreeNode<T> root) {
        throw new NotImplementedException();
    }
}
