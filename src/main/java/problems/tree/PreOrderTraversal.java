package problems.tree;

import java.util.List;
import exceptions.NotImplementedException;
import structures.tree.BinaryTreeNode;

/**
 * Dada la raíz de un árbol binario, retorna el recorrido en pre-orden de los valores de sus nodos.
 * (Raíz -> Izquierda -> Derecha)
 *
 * <pre>
 * Ejemplo:
 *        1
 *       / \
 *      2   3
 *     / \
 *    4   5
 *
 * Recorrido Pre-orden: [1, 2, 4, 5, 3]
 * </pre>
 */
public class PreOrderTraversal {

    /**
     * Resuelve el problema.
     *
     * @param <T> El tipo de dato almacenado en el árbol.
     * @param root La raíz del árbol binario.
     * @return Una lista con los valores del recorrido en pre-orden.
     */
    public <T> List<T> solve(BinaryTreeNode<T> root) {
        throw new NotImplementedException();
    }
}
