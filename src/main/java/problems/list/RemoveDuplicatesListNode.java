package problems.list;

import exceptions.NotImplementedException;
import structures.list.ListNode;

/// Dada la cabeza de un {@link ListNode} de valores no ordenados, elimina los
/// valores duplicados en memoria conservando solo su primera aparición.
///
/// | Original             | Esperado        |
/// |----------------------|-----------------|
/// | 90, 1, 1, 2, 2, -100 | 90->1->2->-100  |
/// | (nulo)               | (nulo)          |
/// | 1, 2, 3              | 1, 2, 3         |
/// | 1, 1, 1, 1, 1        | 1 |
public class RemoveDuplicatesListNode {

    /**
     * @param <T>  El tipo de dato de los elementos en la lista
     * @param head El nodo cabeza de la lista
     */
    public <T> void solve(ListNode<T> head) {
        throw new NotImplementedException();
    }
}
